{
  "name": "retro-board-server",
  "version": "1.0.0",
  "description": "Real-time retrospective board backend",
  "license": "MIT",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "npx nodemon src/index.js",
    "gen-api": "node -e \"const swaggerJsdoc = require('swagger-jsdoc'); const options = require('./src/swaggerOptions.js'); const fs = require('fs'); fs.writeFileSync('../openapi.json', JSON.stringify(swaggerJsdoc(options), null, 2));\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "multer": "^2.1.1",
    "socket.io": "^4.7.4",
    "sqlite3": "^5.1.7",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "swagger-jsdoc": "^6.2.8"
  }
}
