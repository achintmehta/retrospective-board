## ADDED Requirements

### Requirement: Add Image to Card
The system SHALL allow users to optionally attach an image when creating a card.

#### Scenario: User creates a card with an image
- **WHEN** user provides an image upload or URL along with their text content
- **THEN** the created card stores the image reference and displays the image alongside the text
