## 1. Refine Layout Container

- [x] 1.1 Increase `max-width` of `.home-main` in `HomePage.css` to allow for multiple columns.
- [x] 1.2 Transition `.boards-grid` from `display: flex` to `display: grid` with `auto-fill` logic.

## 2. Card Visual Updates

- [x] 2.1 Update `.board-card` styling to include more padding and centered layout.
- [x] 2.2 Re-position the delete button on the card for a more modern appearance.
- [x] 2.3 Refine hover interactions (shadows/transforms) for a distinct "card" feel.

## 3. Verification

- [x] 3.1 Verify grid responsiveness by resizing the browser.
- [x] 3.2 Confirm room icons and names are properly aligned within the new grid cells.
